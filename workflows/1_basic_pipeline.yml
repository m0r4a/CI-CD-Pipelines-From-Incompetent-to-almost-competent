name: 1_basic-pipeline

on: [push]

jobs:

  print_hello_world:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.13.7'

      - run: python scripts/pipeline_1/hello_world.py
